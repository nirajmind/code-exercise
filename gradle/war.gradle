apply plugin: "war"

bootWar {
    mainClassName = "niraj.code.ExerciseApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
